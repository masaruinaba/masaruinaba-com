{
  "name": "balloon-portfolio",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4190 --strictPort",
    "build": "vite build && node sync-portfolio.mjs",
    "test": "node --test *.test.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.32",
    "three": "^0.180.0",
    "vite": "^7.0.0"
  },
  "private": true
}
